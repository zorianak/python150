from typing import List, Optional


class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

"""
You are given the head of a singly linked-list. Reorder it such that L0 → Ln → L1 → Ln-1 → L2 → Ln-2 → …
"""


class Solution:
    def reorderList(self, head: Optional[ListNode]) -> None:
        pass
